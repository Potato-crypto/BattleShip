<Window x:Class="BattleShip.Client.GameOverWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Игра окончена" 
        Height="550" 
        Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">
    
    <Window.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#1a1a2e" Opacity="0.8"/>
    </Window.Effect>
    
    <Border CornerRadius="20" BorderBrush="#4a69bd" BorderThickness="2">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0c2461" Offset="0"/>
                <GradientStop Color="#1e3c72" Offset="0.5"/>
                <GradientStop Color="#0c2461" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок -->
            <Border Grid.Row="0" 
                    Background="#4a69bd" 
                    CornerRadius="20,20,0,0"
                    Padding="20">
                <TextBlock x:Name="TitleText"
                           Text="ИГРА ОКОНЧЕНА"
                           Foreground="White"
                           FontSize="28"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"/>
            </Border>
            
            <!-- Основное содержимое -->
            <StackPanel Grid.Row="1" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        Margin="20">
                
                <!-- Иконка победителя/проигравшего -->
                <Border x:Name="ResultIcon"
                        Width="80" 
                        Height="80" 
                        CornerRadius="40" 
                        Background="#4a69bd" 
                        HorizontalAlignment="Center"
                        Margin="0,0,0,20">
                    <TextBlock x:Name="IconText"
                               Text="🏆"
                               Foreground="White"
                               FontSize="40"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                </Border>
                
                <!-- Результат -->
                <TextBlock x:Name="ResultText"
                           Text="Вы победили!"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           TextWrapping="Wrap"
                           TextAlignment="Center"
                           Margin="0,0,0,10"/>
                
                <!-- Статистика -->
                <Border Background="#1e3c72"
                        CornerRadius="10"
                        Padding="15"
                        Margin="0,10">
                    <StackPanel>
                        <TextBlock x:Name="StatsText"
                                   Text="Статистика:"
                                   Foreground="#8aace8"
                                   FontSize="14"
                                   Margin="0,0,0,5"/>
                        
                        <StackPanel x:Name="StatsPanel">
                            <!-- Статистика будет добавлена динамически -->
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
            
            <!-- Кнопки -->
            <StackPanel Grid.Row="2" 
                        Orientation="Horizontal"
                        HorizontalAlignment="Center"
                        Margin="0,0,0,20">
                
                <!-- Кнопка "Играть еще раз" -->
                <Button x:Name="PlayAgainButton"
                        Content="🎮 Играть еще раз"
                        Width="180"
                        Height="50"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="White"
                        Background="#4a69bd"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="PlayAgainButton_Click"
                        Margin="10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#4a69bd" Opacity="0.5"/>
                    </Button.Effect>
                </Button>
                
                <!-- Кнопка "Выйти из игры" -->
                <Button x:Name="ExitButton"
                        Content="🚪 Выйти в меню"
                        Width="180"
                        Height="50"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="White"
                        Background="#e74c3c"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="ExitButton_Click"
                        Margin="10,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                    <Button.Effect>
                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Color="#e74c3c" Opacity="0.5"/>
                    </Button.Effect>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>