<Window x:Class="BattleShip.Client.GameWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Морской Бой - Подготовка к игре" 
        Height="800" 
        Width="1000"
        WindowStartupLocation="CenterScreen"
        Background="#0c2461">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок с кнопкой назад -->
        <Border Grid.Row="0" 
                Background="#1e3c72" 
                Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Кнопка назад -->
                <Button Grid.Column="0"
                        Content="← Назад"
                        Width="100"
                        Height="35"
                        FontSize="14"
                        FontWeight="Bold"
                        Foreground="White"
                        Background="#4a69bd"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="BackButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <!-- Заголовок -->
                <TextBlock Grid.Column="1"
                           Text="МОРСКОЙ БОЙ"
                           Foreground="White"
                           FontSize="24"
                           FontWeight="Bold"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>

                <!-- Статус подключения -->
                <Border Grid.Column="2"
                        Background="#4a69bd"
                        CornerRadius="15"
                        Padding="10,5">
                    <TextBlock x:Name="ConnectionStatus"
                               Text="Не подключено"
                               Foreground="White"
                               FontSize="12"
                               FontWeight="Bold"/>
                </Border>
            </Grid>
        </Border>

        <!-- Основное содержимое - два поля -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Левое поле - Ваше поле -->
            <Border Grid.Column="0"
                    Background="#16213e"
                    CornerRadius="10"
                    BorderBrush="#4a69bd"
                    BorderThickness="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок левого поля -->
                    <Border Grid.Row="0"
                            Background="#4a69bd"
                            CornerRadius="10,10,0,0"
                            Padding="10">
                        <TextBlock Text="ВАШЕ ПОЛЕ"
                                  Foreground="White"
                                  FontSize="18"
                                  FontWeight="Bold"
                                  HorizontalAlignment="Center"/>
                    </Border>

                    <!-- Игровое поле (сетка 10x10) -->
                    <Border Grid.Row="1"
                            Margin="20"
                            BorderBrush="#6a89cc"
                            BorderThickness="1"
                            Background="#0f3460"
                            CornerRadius="5">
                        <Grid x:Name="YourBoardGrid"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="10">
                            <!-- Сетка будет создана в коде -->
                        </Grid>
                    </Border>

                    <!-- Корабли для расстановки -->
                    <Border Grid.Row="2"
                            Background="#1e3c72"
                            CornerRadius="0,0,10,10"
                            Padding="10">
                        <StackPanel>
        
                            <!-- Кнопка очистки поля -->
                            <Button x:Name="ClearBoardButton"
                                    Content="🗑️ Очистить поле"
                                    Width="200"
                                    Height="35"
                                    Background="#e74c3c"
                                    Foreground="White"
                                    FontSize="13"
                                    FontWeight="Bold"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Margin="0,0,0,10"
                                    Click="ClearBoardButton_Click">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
        
                            <!-- Информация о кораблях -->
                            <TextBlock x:Name="ShipsInfo"
                                       Text="Осталось расставить: 1x4, 2x3, 3x2, 4x1"
                                       Foreground="White"
                                       FontSize="12"
                                       HorizontalAlignment="Center"
                                       Margin="0,0,0,10"/>
        
                            <!-- Кнопка случайной расстановки -->
                            <Button x:Name="RandomPlacementButton"
                                    Content="🎲 Расставить случайно"
                                    Width="200"
                                    Height="35"
                                    Background="#4a69bd"
                                    Foreground="White"
                                    FontSize="13"
                                    FontWeight="Bold"
                                    BorderThickness="0"
                                    Cursor="Hand"
                                    Click="RandomPlacementButton_Click">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="5"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>

            <!-- Разделитель -->
            <Border Grid.Column="1"
                    Background="#4a69bd"
                    Width="2"
                    Opacity="0.3"/>

            <!-- Правое поле - Поле противника -->
            <Border Grid.Column="2"
                    Background="#16213e"
                    CornerRadius="10"
                    BorderBrush="#4a69bd"
                    BorderThickness="2">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Заголовок правого поля -->
                    <Border Grid.Row="0"
                            Background="#4a69bd"
                            CornerRadius="10,10,0,0"
                            Padding="10">
                        <TextBlock Text="ПОЛЕ ПРОТИВНИКА"
                                  Foreground="White"
                                  FontSize="18"
                                  FontWeight="Bold"
                                  HorizontalAlignment="Center"/>
                    </Border>

                    <!-- Игровое поле противника -->
                    <Border Grid.Row="1"
                            Margin="20"
                            BorderBrush="#6a89cc"
                            BorderThickness="1"
                            Background="#0f3460"
                            CornerRadius="5">
                        <Grid x:Name="OpponentBoardGrid"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="10">
                            <!-- Сетка будет создана в коде -->
                        </Grid>
                    </Border>

                    <!-- Панель кнопок под правым полем -->
                    <StackPanel Grid.Row="2"
                                Background="#1e3c72">

                        <!-- Кнопка "Играть с другом" -->
                        <Button x:Name="PlayWithFriendButton"
                                Content="🎮 Играть с другом"
                                Height="45"
                                Background="#4a69bd"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="16"
                                FontWeight="Bold"
                                Cursor="Hand"
                                Click="PlayWithFriendButton_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <!-- Кнопка "Случайный соперник" -->
                        <Button x:Name="RandomOpponentButton"
                                Content="🎲 Случайный соперник"
                                Height="45"
                                Background="#6a89cc"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="16"
                                FontWeight="Bold"
                                Cursor="Hand"
                                Click="RandomOpponentButton_Click">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="8"/>
                                </Style>
                            </Button.Resources>
                        </Button>

                        <!-- Индикатор поиска (скрыт по умолчанию) -->
                        <Border x:Name="SearchIndicator"
                                Background="#1e3c72"
                                CornerRadius="8"
                                Padding="15"
                                Visibility="Collapsed">
                            <StackPanel HorizontalAlignment="Center">
                                <!-- Анимация поиска -->
                                <Grid Margin="0,0,0,10">
                                    <Ellipse Width="40" Height="40" Stroke="#4a69bd" StrokeThickness="3">
                                        <Ellipse.Triggers>
                                            <EventTrigger RoutedEvent="Loaded">
                                                <BeginStoryboard>
                                                    <Storyboard RepeatBehavior="Forever">
                                                        <DoubleAnimation
                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
                                                            From="0" To="360" Duration="0:0:2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </EventTrigger>
                                        </Ellipse.Triggers>
                                        <Ellipse.RenderTransform>
                                            <RotateTransform CenterX="20" CenterY="20"/>
                                        </Ellipse.RenderTransform>
                                    </Ellipse>
                                    <TextBlock Text="🔍"
                                              FontSize="20"
                                              HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                                </Grid>
                                
                                <TextBlock x:Name="SearchStatus"
                                          Text="Поиск соперника..."
                                          Foreground="White"
                                          FontSize="14"
                                          HorizontalAlignment="Center"/>
                                
                                <ProgressBar x:Name="SearchProgress"
                                            Height="8"
                                            Width="200"
                                            Margin="0,10,0,0"
                                            IsIndeterminate="True"
                                            Background="#0f3460"
                                            Foreground="#4a69bd"/>
                                
                                <Button x:Name="CancelSearchButton"
                                        Content="Отменить поиск"
                                        Width="150"
                                        Height="35"
                                        Margin="0,10,0,0"
                                        Background="#e74c3c"
                                        Foreground="White"
                                        FontSize="12"
                                        FontWeight="Bold"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        Click="CancelSearchButton_Click">
                                    <Button.Resources>
                                        <Style TargetType="Border">
                                            <Setter Property="CornerRadius" Value="5"/>
                                        </Style>
                                    </Button.Resources>
                                </Button>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- Статус игры внизу -->
        <Border Grid.Row="2"
                Background="#1e3c72"
                Padding="10">
            <TextBlock x:Name="GameStatus"
                      Text="Подготовка к игре - расставьте корабли на вашем поле"
                      Foreground="White"
                      FontSize="14"
                      HorizontalAlignment="Center"/>
        </Border>
    </Grid>
</Window>