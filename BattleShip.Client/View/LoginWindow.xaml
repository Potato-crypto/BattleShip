<Window x:Class="BattleShip.Client.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Вход в Морской Бой" 
        Height="400" 
        Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="True">
    
    <Window.Effect>
        <DropShadowEffect BlurRadius="20" ShadowDepth="0" Color="#1a1a2e" Opacity="0.8"/>
    </Window.Effect>
    
    <Border CornerRadius="20"  BorderBrush="#4a69bd" BorderThickness="2">
        <Border.Background>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#0c2461" Offset="0"/>
                <GradientStop Color="#1e3c72" Offset="0.5"/>
                <GradientStop Color="#0c2461" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок с кнопкой назад -->
            <Border Grid.Row="0" 
                    Background="#4a69bd" 
                    CornerRadius="20,20,0,0"
                    Padding="15">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Кнопка назад -->
                    <Button Grid.Column="0"
                            Content="←"
                            Width="40"
                            Height="40"
                            FontSize="20"
                            FontWeight="Bold"
                            Foreground="White"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="BackButton_Click"/>
                    
                    <!-- Заголовок -->
                    <TextBlock Grid.Column="1"
                               Text="Вход в аккаунт"
                               Foreground="White"
                               FontSize="22"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>
                    
                    <!-- Кнопка закрытия -->
                    <Button Grid.Column="2"
                            Content="✕"
                            Width="30"
                            Height="30"
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="White"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseButton_Click"/>
                </Grid>
            </Border>
            
            <!-- Основное содержимое -->
            <StackPanel Grid.Row="1" 
                        VerticalAlignment="Center" 
                        HorizontalAlignment="Center"
                        Width="350">
                
                <!-- Иконка -->
                <Border Width="80" 
                        Height="80" 
                        CornerRadius="40" 
                        Background="#4a69bd" 
                        HorizontalAlignment="Center">
                    <TextBlock Text="👤"
                              Foreground="White"
                              FontSize="40"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"/>
                </Border>
                
                <!-- Кнопка входа через Google -->
                <Button x:Name="GoogleLoginButton" 
                        Content="Войти через Google"
                        Height="55"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="#333333"
                        Background="White"
                        BorderThickness="1"
                        BorderBrush="#dddddd"
                        Cursor="Hand"
                        Click="GoogleLoginButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="10" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="15,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Иконка Google -->
                                    <TextBlock Grid.Column="0"
                                               Text="G"
                                               FontSize="20"
                                               FontWeight="Bold"
                                               Foreground="#4285F4"
                                               Margin="0,0,15,0"
                                               VerticalAlignment="Center"/>
                                    
                                    <TextBlock Grid.Column="1"
                                               Text="{TemplateBinding Content}"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontSize="{TemplateBinding FontSize}"
                                               FontWeight="{TemplateBinding FontWeight}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <!-- Или -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>
                    
                    <Line Grid.Column="0" 
                          Stroke="#4a69bd" 
                          StrokeThickness="1" 
                          VerticalAlignment="Center"
                          Opacity="0.5"/>
                    
                    <TextBlock Grid.Column="1"
                               Text="или"
                               Foreground="#8aace8"
                               FontSize="12"
                               Margin="10,0"
                               VerticalAlignment="Center"/>
                    
                    <Line Grid.Column="2" 
                          Stroke="#4a69bd" 
                          StrokeThickness="1" 
                          VerticalAlignment="Center"
                          Opacity="0.5"/>
                </Grid>
                
                <!-- Вход через почту -->
                <Button x:Name="EmailLoginButton" 
                        Content="Войти через почту"
                        Height="55"
                        FontSize="16"
                        FontWeight="Bold"
                        Foreground="White"
                        Background="#4a69bd"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="EmailLoginButton_Click">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="10"/>
                        </Style>
                    </Button.Resources>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border CornerRadius="10" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    Padding="15,0">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <!-- Иконка почты -->
                                    <TextBlock Grid.Column="0"
                                               Text="✉"
                                               FontSize="20"
                                               Margin="0,0,15,0"
                                               VerticalAlignment="Center"/>
                                    
                                    <TextBlock Grid.Column="1"
                                               Text="{TemplateBinding Content}"
                                               Foreground="{TemplateBinding Foreground}"
                                               FontSize="{TemplateBinding FontSize}"
                                               FontWeight="{TemplateBinding FontWeight}"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <!-- Регистрация -->
                <StackPanel Margin="0,20,0,0">
                    <TextBlock Text="Нет аккаунта?"
                              Foreground="#8aace8"
                              FontSize="14"
                              HorizontalAlignment="Center"/>
                    
                    <Button x:Name="RegisterButton"
                            Content="Зарегистрироваться"
                            Height="40"
                            FontSize="14"
                            FontWeight="Bold"
                            Foreground="#4a69bd"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="RegisterButton_Click"/>
                </StackPanel>
            </StackPanel>
            
            <!-- Нижняя панель -->
            <Border Grid.Row="2" 
                    Background="#4a69bd" 
                    CornerRadius="0,0,20,20"
                    Padding="10">
                <TextBlock Text="Выберите способ входа в систему"
                          Foreground="White"
                          FontSize="12"
                          HorizontalAlignment="Center"/>
            </Border>
        </Grid>
    </Border>
</Window>
